# Set default behavior to automatically handle line endings.
* text=auto

# Explicitly declare text files to be normalized.
# These will be stored with LF endings in the repository and converted to CRLF on Windows checkout.
*.py text
*.md text
*.txt text
*.json text
*.yaml text
*.yml text
*.spec text
*.ini text
*.bat text
.gitignore text
.gitattributes text

# Declare files that should always have LF line endings, regardless of checkout platform.
*.sh eol=lf

# Declare files that are binary and should not be modified.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pickle binary
*.exe binary
*.zip binary
*.dll binary
*.so binary
*.dylib binary

# Python specific binary files
*.pyc binary
*.pyo binary
*.pyd binary

# Configuration files that may need platform-specific endings
WindowsConfig/*.txt text eol=crlf
LinuxConfig/*.txt text eol=lf